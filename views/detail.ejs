<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>detail</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Sarabun:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800&display=swap"
        rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #430f74, #872af2);
            color: #e4e4e6;
            font-family: Sarabun;
        }

        /*ตัวอักษรและรูปภาพ*/
        img {
            width: 300px;
            height: 450px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
        }

        h3 {
            font-size: 22px;
            color: #430f74;
        }

        h1 {
            text-align: center;
            font-size: 30px;
            color: #430f74;
            margin-bottom: 30px;
        }

        h2 {
            text-align: left;
            font-size: 20px;
            color: #430f74;
            margin-bottom: 20px;
        }

        /*แถบบาร์*/
        .navbar {
            background-color: #ffffff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            border-bottom: 2px solid #e0e0e0;
            border-radius: 10px;
        }

        .navbar-right a {
            font-size: 22px;
            color: #6e22b7;
            text-decoration: none;
            margin: 0 10px;
        }

        .navbar-center a {
            font-size: 18px;
            color: #333;
            text-decoration: none;
            margin: 0 15px;
            font-weight: bold;
        }

        .navbar a:hover {
            color: goldenrod;
        }

        .details-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 20px auto;
            padding: 20px;
            background-color: #1e1e1e;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            max-width: 600px;
        }

        .container {
            width: 80%;
            margin: 20px auto;
            background-color: white;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .movie {
            display: flex;
            align-items: flex-start;
            gap: 20px;
        }

        .movie-image img {
            width: 300px;
        }

        .movie-info {
            margin: 10px 0;
            font-size: 22px;
        }

        .movie-info span {
            display: block;
            color: #430f74;
        }

        .synopsis h2 {
            margin-top: 20px;
            color: #430f74;
        }

        .synopsis p {
            font-size: 20px;
            text-align: justify;
            color: #430f74;
        }

        hr {
            border: none;
            height: 1px;
            background-color: #430f74;
            margin: 20px 0;
        }

        .details-btn,
        .details-btn a {
            background-color: #872af2;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            margin: 5px 0;
            display: inline-block;
        }

        .details-btn:hover,
        .details-btn a:hover {
            background-color: #430f74;
        }

        /* Media Queries */
        @media (max-width: 768px) {
            .movie {
                flex-direction: column;
                align-items: center;
                text-align: center;
            }

            .movie-image img {
                width: 200px;
                height: auto;
                /* ปรับอัตราส่วนภาพให้เหมาะสม */
            }

            h1 {
                font-size: 24px;
            }

            h2 {
                font-size: 18px;
            }

            .movie-info {
                font-size: 16px;
            }

            .details-btn {
                padding: 8px 15px;
                font-size: 16px;
            }
        }

        @media (max-width: 480px) {
            .navbar {
                flex-direction: column;
                align-items: flex-start;
            }

            .navbar-center a,
            .navbar-right a {
                font-size: 16px;
                margin: 5px 0;
            }

            h1 {
                font-size: 20px;
            }

            .movie-info {
                font-size: 14px;
            }

            .details-btn {
                padding: 6px 10px;
                font-size: 14px;
            }

            hr {
                margin: 15px 0;
            }
        }
    </style>

    <script>
        function confirmLogout() {
            let confirmed = confirm("คุณต้องการลงชื่อออกหรือไม่?");
            if (!confirmed) {
                return false; 
            }
            window.location.href = "/logout"; 
        }
    </script>
</head>

<body>
    <nav class="navbar">
        <header>
            <h1>โรงภาพยนตร์</h1>
        </header>
        <div class="navbar-center">
            <a href="/main">หน้าหลัก</a>
            <a href="/movie">ภาพยนตร์</a>
            <a href="/allhistory">ประวัติการจอง</a>
        </div>
        <div class="navbar-right">
            <% if (user) { %>
                <a href="#" style="font-weight: 800;" onclick="confirmLogout()">ยินดีต้อนรับ, <%= user.username %>
                        !</a>
                <% } else { %>
                    <a href="/login">เข้าสู่ระบบ/สมัครสมาชิก</a>
                    <% } %>
        </div>
    </nav>
    <% data.forEach((item)=> { %>
        <div class="container">
            <div class="movie">
                <div class="movie-image">
                    <img src="/img/<%=item.MovieID%>.jpg" alt="<%= item.Title %>" />
                </div>
                <div class="movie-details">
                    <h1>
                        <%= item.Title %>
                    </h1>
                    <div class="movie-info">
                        <span>วันที่เข้าฉาย: <%= item.ReleaseDate %></span>
                        <span>หมวดหมู่: <%= item.Genre %></span>
                        <span>ระยะเวลา: <%= item.Duration %> นาที</span>
                        <span>คะแนน: <%= item.Rating %></span>
                        <span>ผู้กำกับ: <%= item.Director %></span>
                    </div>
                    <hr>
                    <div class="synopsis">
                        <h2>เรื่องย่อ:</h2>
                        <p>
                            <%= item.Synopsis %>
                        </p>
                    </div>
                    <a href="/booking?MovieID=<%=item.MovieID%>" class="details-btn">ซื้อตั๋วภาพยนตร์</a>
                    <a href="/main" class="details-btn">กลับหน้าหลัก</a>
                </div>
            </div>
        </div>
        <% }); %>
</body>

</html>